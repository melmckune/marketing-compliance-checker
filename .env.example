# Get this from the Neon dashboard (or `vercel env pull` once linked to a
# Vercel project with the Neon integration attached). Use the pooled
# connection string.
DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/marketing-compliance-checker?sslmode=require"
